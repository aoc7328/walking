# 多人版 Worker 專用。build:worker 會跑 `vite build --mode worker`，
# Vite 讀 .env.worker.local 覆蓋掉 .env.local 的同名變數。
#
# 為什麼要分開：.env.local 那把 Maps key 的來源限制是 walking2.pages.dev，
# 兩邊共用會互相打架——多人版用會被擋，反過來把它換成新 key 又會弄壞你自己的 Pages 版。
#
# 用法：複製成 .env.worker.local（已在 .gitignore），填入新建的那把 key。
VITE_GOOGLE_MAPS_API_KEY=
